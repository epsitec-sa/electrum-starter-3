'use strict';

export default function (theme) {
  return {
    base: {
      display: 'flex',
      flexDirection: 'column',
      minWidth: '100%',
      maxWidth: '100%',
      margin: '15px 12px 15px 12px',
      whiteSpace: 'nowrap'
    }
  };
}

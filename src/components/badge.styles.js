'use strict';

export default function (theme) {
  return {
    base: {
      backgroundColor: '#f00',
      color: '#fff',
      borderRadius: '2em',
      fontSize: '75%',
      fontWeight: 'bold',
      padding: '0.4em'
    }
  };
}

'use strict';

export default function (theme) {
  return {
    base: {
      display: 'flex',
      flexDirection: 'row',
      alignItems: 'center',
      justifyContent: 'space-between',
      marginTop: '5px',
      marginBottom: '5px',
      width: '100%'
    }
  };
}

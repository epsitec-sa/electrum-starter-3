{
  "name": "electrum-starter-3",
  "version": "2.4.0",
  "description": "",
  "main": "src/index.js",
  "private": true,
  "scripts": {
    "babel": "babel --quiet --out-dir lib src && babel --quiet --out-dir lib.test src.test",
    "clean": "rimraf ./lib && rimraf ./lib.test",
    "compile": "npm run clean && npm run regen && npm run babel",
    "mocha": "cross-env BABEL_ENV=test mocha lib.test/**/*.js",
    "prepublish": "npm run compile",
    "regen": "require-self && electrum-require-components --wrap ./src components .component.js all-components.js",
    "start": "node webpack.devserver.js",
    "test": "npm run compile && npm run mocha",
    "watch": "webpack --display-error-details --watch --progress --colors -d",
    "webpack": "webpack --display-error-details -d"
  },
  "author": "Epsitec SA",
  "license": "MIT",
  "dependencies": {
    "electrum": "^4.3.0",
    "electrum-arc": "^3.2.1",
    "electrum-command": "^2.2.0"
  },
  "peerDependencies": {
    "react": "^15.0.2"
  },
  "devDependencies": {
    "babel-env": "^1.2.2",
    "babel-loader": "^6.2.4",
    "cross-env": "^1.0.7",
    "electrum-require-components": "^1.6.2",
    "electrum-style-reload": "^1.2.0",
    "mai-chai": "^2.6.0",
    "material-ui": "^0.15.0",
    "react-transform-catch-errors": "^1.0.2",
    "react-transform-hmr": "^1.0.4",
    "redbox-react": "^1.2.4",
    "require-self": "^0.1.0",
    "rimraf": "^2.5.2",
    "webpack": "^1.13.0",
    "webpack-dev-server": "^1.14.1"
  }
}
